fuller stack tests
features:
  delete old snaps
  hashes for syncing only what's changed
  gc
  make rename safer
backends:
  support adding a second backend later (syncing)
other TODOs in comments
performance
  offset and length for backends
