fuller stack tests
features:
  hashes for syncing only what's changed
  hashes for SIS
  gc
  make rename safer
  fix symlinks
backends:
  support adding a second backend later (syncing)
other TODOs in comments
