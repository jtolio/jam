fuller stack tests
cache:
  more testing
  preserve misra-gries and LRU state somewhere
SIS:
  flush hashes when blobs are flushed
  maybe hashes should be in a blob header?
features:
  gc
  make rename safer
  url sharing export
backends:
  support adding a second backend later (syncing)
other TODOs in comments
