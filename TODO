fuller stack tests
features:
  delete old snaps
  hashes for syncing only what's changed
  gc
backends:
  allow other backends to register themselves
  support prefixes in backends
other TODOs in comments
performance
